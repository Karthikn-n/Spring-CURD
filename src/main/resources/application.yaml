#spring.application.name=HelloWorld
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/curd
    username: postgres
    password: dbadmin
#    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080